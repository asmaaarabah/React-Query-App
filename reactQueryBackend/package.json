{
  "name": "reactquerybackend",
  "version": "1.0.0",
  "description": "json server backend",
  "main": "index.js",
  "scripts": {
    "start": "json-server -w db.json --port 5005",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
